import './spinner.scss';

const Spinner = () => {
    return(
        <div className="spinnerWrapper">
            <img src="./spinner.gif" alt="spinner" />
        </div>
    )
}

export default Spinner;